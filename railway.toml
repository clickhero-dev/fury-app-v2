[build]
builder = "nixpacks"
buildCommand = "npm install --include=dev && npm run build --workspace=packages/shared && npm run build --workspace=packages/db && npm run build --workspace=@fury/api"

[deploy]
startCommand = "node apps/api/dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
