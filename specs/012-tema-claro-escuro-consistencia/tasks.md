# Tasks: Consistência de Tema Claro/Escuro — Correção Definitiva

**Input**: Design documents from `specs/012-tema-claro-escuro-consistencia/` (spec.md, plan.md)

**Status desta fase**: Nenhuma task abaixo foi executada. Esta sessão produziu o plano (`spec.md` + `plan.md` + este `tasks.md`) e, após o usuário pedir explicação de impacto/necessidade, **trouxe KF-001 (`Button.tsx`) e KF-002 (21 ocorrências de contraste, número corrigido após a 3ª verificação — ver abaixo) para dentro da execução** — ver `plan.md`, seções "Achado de contraste — decisão de escopo (KF-002) — REVISADA" e "Decisão sobre Button.tsx (KF-001)". **A implementação é a próxima fase**, fora do escopo desta entrega — nenhum código foi alterado.

**Nota sobre "specs futuras" citadas neste documento**: várias tasks abaixo apontam achados fora de escopo para "uma spec futura". Isso NÃO é uma única spec — são pelo menos três frentes distintas, não confundir: (1) **remoção do bloco `html:not(.dark)`** (Abordagem A do `plan.md`, depende de auditar os 26 arquivos da Fase 1) — candidata real a `specs/013-...`; (2) **conclusão do rebrand FURY→ady** (KF-007 — `pages/estudio/*`, `components/campaign-wizard/*`, focus rings do design system — não tem nada a ver com CSS/tema, é troca de cor de marca) — precisa da própria spec, numeração própria quando for aberta; (3) **contraste AA remanescente** (KF-004, KF-006, KF-009 — mesma família de problema do KF-002 desta spec, mas volume grande demais pra entrar aqui) — pode virar uma spec própria ou ser anexado a uma futura extensão do KF-002. As tasks abaixo foram corrigidas para não rotular as três a mesma coisa.

**Ordenação**: por risco crescente de regressão visual, não por prioridade de produto. Fases 1-2 são leitura/auditoria (risco zero). Fases 3-6 tocam arquivos isolados (risco baixo/local, sem CSS compartilhado). Nenhuma fase remove ou reescreve `index.css:322-462` como um todo — só o `plan.md` (Abordagem C) autoriza correções pontuais e aditivas fora dele. Fases 7-9 são verificação. Fase 10 é fechamento.

## Formato: `[ID] [Risco] Descrição`

- Caminhos de arquivo exatos incluídos em cada task.
- Critério de aceite (AC) ao final de cada task.
- Cada fase termina com um **checklist de saída** — não avance para a próxima fase sem marcar todos os itens.

---

## Fase 1 — Auditoria prévia (risco zero, só leitura)

**Objetivo**: mapear o raio de impacto real do bloco CSS genérico e revalidar os achados antes de qualquer mudança, para informar o rollout e a futura spec 013 (remoção do bloco).

- [x] **T001** `[EXECUTADO 2026-08-26]` `grep -rl 'bg-gray-\|bg-zinc\|bg-slate\|bg-neutral\|bg-stone' apps/web/src` → **24 arquivos**, bate exatamente com a tabela já documentada em `plan.md`/`spec.md` (nenhuma discrepância). Confirma que a maioria usa só `bg-gray-*` (não coberto pelo bloco `html:not(.dark)` — coberto por um bloco `.dark{}` separado) e que só `AssinaturaVencida.tsx` (já em escopo) tem dependência real do bloco de modo claro. Nenhum arquivo alterado.
- [x] **T002** `[EXECUTADO 2026-08-26 — DISCREPÂNCIA REAL ENCONTRADA]` `grep -rn 'text-\[#1E88A8\]' apps/web/src` revalidado. **Achado novo, fora da tabela revisada**: `apps/web/src/pages/dashboard/Dashboard.tsx:632` — `<Link to="/integracoes" className="inline-flex items-center gap-1 text-sm font-medium text-[#1E88A8] hover:underline">Ir para integrações ...</Link>` — é `<Link>` (efeito visual real, sem mascaramento), rota alcançável em modo claro, não estava na tabela de 21 ocorrências. **Adicionado a T030 abaixo.** Contagem corrigida: **22 ocorrências confirmadas em 9 arquivos** (era 21). Resto da lista bate com o documentado. `text-brand`/`text-admin-petrol` (KF-009) não expandidos para esta fase, conforme decidido.
- [x] **T003** `[EXECUTADO 2026-08-26]` `grep -rn "AuthLayout" apps/web/src` → só `components/index.ts:10,28` (comentário + export) e o próprio `components/AuthLayout.tsx`. Confirmado sem novos imports. Seguro prosseguir para T040 quando chegar a Fase 5.
- [x] **T004** `[EXECUTADO 2026-08-26]` `grep -rn '"/painel"' apps/web apps/api docs` → **zero resultados** (nem o EasyPanel do `docs/DEPLOY.md` bateu com essa forma exata de busca — confirma ainda mais forte que não há rota interna `/painel`). Sem task de código criada.
- [x] **T005** `[EXECUTADO 2026-08-26 — DISCREPÂNCIA GRANDE ENCONTRADA]` `grep -rln "from '@/components/ui/button'" apps/web/src` → confirma os 9 arquivos já documentados. **Mas** o grep de `variant="outline"|variant="soft"` retornou 33 ocorrências em 16 arquivos, não 16 em 9 — muitos desses arquivos (`CreativeStudio.tsx`, `GeradorCopy.tsx`, `BudgetSuggestionCard.tsx`, etc.) não importam `Button` pelo caminho direto `@/components/ui/button`, e sim via **barrel export** (`from '@/components'`) ou **import relativo** (`from '../ui/button'`) — caminhos que o grep original nunca cobriu. Levantamento completo, confirmando que todos realmente renderizam `<Button` (não é import morto):
  - **Diretos (9)**: `CampaignWizard.tsx`, `InstagramPostsTab.tsx`, `Step5Review.tsx`, `Plans.tsx`, `Subscription.tsx`, `PublicoContent.tsx`, `ConectarMetaPage.tsx`, `MetasPage.tsx`, `CalendarView.tsx`.
  - **Via barrel `@/components` (14, NOVO)**: `FuryRuleDialog.tsx`, `MinhasRegras.tsx`, `ComponentsDemo.tsx`, `BrandKitPage.tsx`, `FuryConfig.tsx`, `estudio/components/AdaptiveQuestions.tsx`, `estudio/components/CreativeFieldsForm.tsx`, `estudio/components/CreativeResult.tsx`, `estudio/components/CreativeWizard.tsx`, `estudio/components/LayoutPicker.tsx`, `estudio/components/LayoutSuggestion.tsx`, `estudio/CreativeStudio.tsx`, `estudio/GeradorCopy.tsx`, `estudio/GeradorImagem.tsx`.
  - **Via import relativo (2, NOVO)**: `components/budget/BudgetModeToggle.tsx`, `components/budget/BudgetSuggestionCard.tsx`.
  - **Total real: 25 arquivos consomem `Button.tsx`, não 9.** A maioria dos 16 novos está no território do KF-007 (módulo Estúdio, nunca rebrandeado) — faz sentido, é o mesmo módulo. **T022 (Fase 3, verificação visual) precisa cobrir os 25, não os 9 originalmente listados** — corrigido abaixo.

### ✅ Checklist de saída — Fase 1

- [x] Tabela de raio de impacto do bloco CSS (24 arquivos, confirmado — número já estava certo) pronta.
- [x] Lista de KF-002 revalidada — **corrigida de 21 para 22 ocorrências / 9 arquivos** (achado: `Dashboard.tsx:632`).
- [x] Lista de consumidores de `Button.tsx` revalidada — **corrigida de 9 para 25 arquivos** (achado: barrel export + import relativo não cobertos pela busca original).
- [x] `AuthLayout.tsx` confirmado sem novos imports.
- [x] `/painel` confirmado sem evidência de bug de código.
- [x] Nenhuma linha de código de produto alterada até aqui — só os documentos da spec.

**Fase 1 concluída em 2026-08-26. Duas discrepâncias reais encontradas e corrigidas nos documentos antes de prosseguir — exatamente o que esta fase existe para pegar.**

---

## Fase 2 — Correções pontuais isoladas (risco baixo, arquivo único, sem CSS compartilhado)

- [x] **T010** `[EXECUTADO 2026-08-26]` `apps/web/src/pages/onboarding/MetaAuthorizePage.tsx:58,107,122` — trocar `!text-[#ECEDEF]` por `text-admin-text` nos três `<h1>`/`<h2>`. AC: elemento herda a cor correta em ambos os temas (claro usa `--admin-text: #0F172A`, escuro usa `--admin-text: #ECEDEF`, conforme `index.css:43,89`); verificado visualmente nos dois temas.
- [x] **T011** `[EXECUTADO 2026-08-26 — abordagem (a) usada: SVG aninhado no radar do 404, reaproveitando a geometria de AdySymbol]` `apps/web/src/pages/NotFoundPage.tsx` — atualizar comentário de cabeçalho (linha ~6-7: remover menção a "FURY... laranja #e8631a"), trocar o `<rect>`+`<text>F</text>` central do SVG (linhas ~104-122) por algo consistente com `AdySymbol`, e trocar o rodapé "FURY — erro 404" (linha ~213) para "ady — erro 404" ou equivalente. **Lido `AdySymbol.tsx` diretamente**: é um componente que renderiza seu PRÓPRIO `<svg viewBox="0 0 32 32">` com um `<g stroke="currentColor" className="text-admin-petrol">` (3 paths formando um "A") + um `<circle className="fill-accent">`. Não é um fragmento `<g>`/`<path>` solto pronto para colar dentro do `<svg>` existente do 404 (que já tem seu próprio `viewBox="0 0 280 180"` com animações de radar) — nesting de `<svg>` dentro de `<svg>` funciona em SVG (basta um `<svg x="122" y="72" width="36" height="36" viewBox="0 0 32 32">` reaproveitando o conteúdo interno de `AdySymbol`), mas `<AdySymbol/>` como componente React não pode ser simplesmente inserido ali sem essa camada extra, porque ele já inclui sua própria tag `<svg>` raiz. Duas abordagens válidas: (a) extrair o `<g>`/`<circle>` internos de `AdySymbol` e colar dentro de um `<svg x=... y=... viewBox="0 0 32 32">` aninhado no SVG do 404 (mantém a animação de radar ao redor); (b) abandonar a composição inline e renderizar `<AdySymbol size={36} />` como elemento HTML posicionado com `absolute`/`transform` por cima do SVG do radar (mais simples, mas exige reposicionar via CSS em vez de coordenadas SVG). Também usa `text-admin-petrol`/`fill-accent`, que são tokens fixos (não theme-conditional, mas cores apropriadas como elemento gráfico nos dois temas — sem risco de contraste, uso decorativo). AC: `grep -i FURY apps/web/src/pages/NotFoundPage.tsx` retorna zero; ícone central visualmente consistente com `AdySymbol` usado em auth/onboarding; página renderizada nos dois temas sem elemento quebrado; decisão entre abordagem (a)/(b) documentada na implementação.
- [x] **T012** `[EXECUTADO 2026-08-26]` `apps/web/src/pages/billing/AssinaturaVencida.tsx` — adicionar variantes `dark:` às classes hardcoded para claro que **não são "gray"** (confirmado empiricamente: `.dark { .bg-gray-*, .text-gray-*, .border-gray-* }` em `index.css:261-317` só cobre nomes literais `gray`, não `slate` — e esse bloco também está fora de `@layer`, `!important`, então onde bate, ganha de qualquer classe nova sem `!`):
  - `bg-[#f3f6f8]` (linha 6, valor arbitrário, não é `gray`) → `+ dark:bg-[#0c0d0a]` — necessário.
  - `text-slate-900/600/500/700/400` (linhas 33,34,52,53,60,63) → `+ dark:text-white`/`dark:text-zinc-*` — necessário, **slate não é gray, nada cobre isso hoje**.
  - `border-slate-200` (linha 38) → `+ dark:border-white/10` — necessário, mesmo motivo.
  - `bg-slate-50` (linha 59, "canais de atendimento") → `+ dark:bg-[#12130F]` — necessário, mesmo motivo.
  - `bg-white` (linha 38, o card) → **NÃO precisa de `dark:bg-[...]` novo**. Confirmado por build real: `.dark .bg-white { background-color: var(--color-surface) !important; }` (`index.css:262`, fora de `@layer`, `!important`) já força a cor certa (`#141512`) sempre que `.dark` estiver ativo — adicionar `dark:bg-[#181915]` seria código morto (a regra genérica sempre vence, um `dark:bg-[...]` sem `!` nunca teria efeito). Deixar `bg-white` como está; o "bug do card" já era só o descasamento com o texto slate hardcoded, resolvido pelos itens acima.
  Seguir o padrão de `RegisterFormPage.tsx`/`LoginPage.tsx` só onde a classe genuinamente precisa de `dark:` (todos os itens "necessário" acima). AC: com tema escuro selecionado, a tela renderiza fundo escuro E texto claro (elimina o bug de "texto quase preto sobre card escuro"); com tema claro, aparência idêntica à atual; confirmar que não sobrou nenhum `dark:bg-[#181915]` morto na classe do card.
- [x] **T013** `[EXECUTADO 2026-08-26]` No mesmo arquivo (`AssinaturaVencida.tsx:79`), resolver a colisão com `index.css:448` (`span:not(.gradient-teal span):not(.gradient-spark span):not(.bg-accent *):not(.bg-accent) { color: #475569 !important; }`). **Não usar uma regra nova competindo por especificidade** (`.cta-label { color: inherit !important }` tem especificidade 0,1,0 — perde de 0,4,3 do seletor original; testado e descartado). Usar a MESMA técnica do T024: adicionar `<span className="cta-label">Ver planos disponíveis</span>` e estender o seletor já existente em `index.css:448` com `:not(.cta-label)`, no final da lista de `:not()` já presente — sem remover nem reescrever a regra, só adicionar mais uma exclusão. AC: texto do botão continua branco sobre fundo `#1E88A8`/`#17708A` em ambos os temas; regra genérica permanece intacta e funcionando para os demais `span`s da aplicação; confirmar que `.cta-label` não colide com nenhuma outra classe existente (`grep -rn "cta-label" apps/web/src` deve retornar só esta ocorrência antes da mudança).
- [x] **T014** `[EXECUTADO 2026-08-26 — valor final: opacity-[0.08]]` `apps/web/src/pages/auth/{LoginPage,RegisterFormPage,ForgotPasswordPage,ResetPasswordPage,ResetPasswordSuccessPage}.tsx` — decidir e aplicar o valor final de opacidade do grid de fundo no modo claro (recomendação do `plan.md`: subir de `opacity-[0.03]` para algo entre `opacity-[0.08]` e `opacity-[0.10]`, mesma cor `#1E88A8`, testado visualmente até o padrão de pontos ficar perceptível sem competir com o conteúdo do card). Aplicar o **mesmo valor** nos 5 arquivos para manter consistência. AC: pontos visíveis a olho nu no modo claro em todos os 5 arquivos, sem parecer "poluição visual" atrás do card; `dark:opacity-[0.05]` inalterado.
- [x] **T015** `[EXECUTADO 2026-08-26, depois do T024]` `apps/web/src/components/auth/GoogleLoginButton.tsx:46` — o botão já define corretamente `hover:border-[#1E88A8]/50 hover:bg-white`, mas é sobrescrito pela mesma regra de `index.css:375-386` que afeta `Button.tsx` (é `type="button"`, sem `.bg-brand`/`.gradient-spark`, dentro do `.flex` de `LoginPage.tsx`/`RegisterPage.tsx`). **Não criar uma marker class nova** — reutilizar `.ady-btn`, a mesma que T024 já adiciona ao `:not()` de `index.css:375-386` (mesmo mecanismo, mesma regra, nenhuma edição de CSS adicional necessária se a classe já existir). Só adicionar `ady-btn` ao `className` do `<button>` deste componente. **Dependência**: T024 precisa ter sido feita antes (ou junto), já que é o que garante que `.ady-btn` está excluído da regra. AC: hover em "Continuar/Cadastrar com Google" mostra borda azul (`border-[#1E88A8]/50`) e fundo branco, não mais o cinza forçado (`#f1f5f9`/`#0f172a`) da regra genérica — testado em `LoginPage.tsx` e `RegisterPage.tsx`.

### ✅ Checklist de saída — Fase 2

- [ ] `MetaAuthorizePage.tsx` legível nos dois temas (T010).
- [ ] `NotFoundPage.tsx` sem nenhuma referência a "FURY" (T011).
- [ ] `AssinaturaVencida.tsx` respeita o toggle de tema, incluindo o botão de CTA com texto branco (T012, T013).
- [ ] Grid de pontos visível no claro em todas as 5 telas de auth (T014).
- [ ] Hover do `GoogleLoginButton` mostra borda azul + fundo branco, não cinza forçado (T015 — **depende de T024 da Fase 3 ter sido feita antes**).
- [ ] Zero linha do bloco `index.css:322-462` alterada (T010, T011, T012, T014 não tocam CSS compartilhado; T013 e T015 só estendem `:not()` já existente, coordenado com T024).

---

## Fase 3 — `Button.tsx` e `Card.tsx` theme-aware (KF-001 + KF-001-B, risco baixo-médio: 2 arquivos mudam, 25 arquivos precisam reverificação — corrigido de 9 na execução real da Fase 1)

**Objetivo**: eliminar a causa raiz do bloco CSS genérico para botões/cards — dar a `apps/web/src/components/ui/button.tsx` e `card.tsx` suporte real aos dois temas, usando tokens semânticos, **e** neutralizar o override que hoje sobrescreve qualquer cor que esses componentes venham a ter dentro de `<main>` (achado da verificação de 2026-08-26 — sem isso, a correção não tem efeito visual).

- [x] **T020** `[EXECUTADO 2026-08-26 — decisões tomadas: default→bg-surface-secondary/border-border-light (drift aceito), ghost→text-text-tertiary (drift aceito), destructive mantido #E5534B próprio (autocontido, não precisa de tema), spark corrigido para #CF6F03, primary→text-white]` Para cada uma das 7 variantes de `button.tsx`, mapear as classes hardcoded atuais para tokens semânticos de `index.css`. Mapeamento variante-a-variante, feito de verdade (não "mapear pra token" genérico):

  | Variante | Valor atual | Token candidato | Situação |
  |---|---|---|---|
  | `default` bg `#1A1B17` | `--color-surface` (escuro: `#141512`) | **Não é match exato** — `#1A1B17` ≠ `#141512`, visualmente próximos mas diferentes. Decisão: aceitar o drift sutil de tom (usar `--color-surface`) ou criar token novo dedicado para preservar o valor exato. |
  | `default` border `#262824` | `--color-border-light` (escuro: `#262824`) | **Match exato.** Usar direto. |
  | `default` text `#ECEDEF` | `--color-text-primary` (escuro: `#ECEDEF`) | **Match exato.** Usar direto. |
  | `ghost` text `#9BA1A6` | `--color-text-secondary` (escuro: `#A3A8B3`) ou `--color-text-tertiary` (escuro: `#8E939D`) | **Nenhum match exato** — os dois candidatos são próximos mas não iguais. Mesma decisão do `default` bg. |
  | `destructive` bg `#E5534B` | `--color-error` (`#da3633`, **não é theme-conditional**, valor único fixo) | **Vermelho DIFERENTE do token.** Usar `--color-error` MUDARIA a cor visível do botão (de `#E5534B` para `#da3633`), não é só adicionar consciência de tema — é decisão de design (unificar com o vermelho do resto do app, ou manter o tom próprio do botão). |
  | `spark` bg `#F97316` | `--color-accent`/`--spark` (`#CF6F03`, a "Faísca" oficial do guia de marca) | **BUG SEPARADO, não é de tema**: `#F97316` é o laranja padrão do Tailwind (`orange-500`), NÃO a Faísca oficial (`#CF6F03`) usada em todo o resto do app (`gradient-spark`, badges, etc.). O botão `spark` nunca usou a cor certa da marca, em nenhum tema. Corrigir para `#CF6F03`/`--color-accent` independentemente da tematização — é acerto de cor, não de tema. |
  | `primary` text `#0C0D0A` | (nenhum token — decisão de design) | A regra global de `index.css:388-398` já força esse texto para branco hoje, coincidentemente (ver achado da 5ª verificação). Decidir: manter a intenção original do componente (texto escuro sobre petróleo) ao tornar `button.tsx` theme-aware de verdade, ou adotar texto branco (o que já é o resultado visual atual, seria "menos surpresa" pra quem já viu o app funcionando)? **Decisão do usuário necessária, não assumir.** |
  | `outline`/`soft` text `#1E88A8` | Ver T021 | Tratado à parte (regra de contraste AA). |

  Para `bg-surface`/`border-border-light`/`text-text-primary`/`text-text-secondary`/`text-text-tertiary` (matches exatos), usar direto. Para os itens "sem match exato" (`default` bg, `ghost` text), decidir entre usar o token próximo (drift sutil aceito) ou criar token novo em `index.css` `@theme` dedicado ao botão. AC: `button.tsx` não tem nenhuma cor hex hardcoded sem par de tema; a cor do `spark` corrigida para `#CF6F03` (bug de marca, não de tema); decisão sobre `primary`'s texto e sobre os 2 casos sem match exato registrada explicitamente (não assumida silenciosamente).
- [x] **T020b** `[EXECUTADO 2026-08-26 — os dois valores tinham match exato de token, sem drift]` Mesma tarefa de T020 para `apps/web/src/components/ui/card.tsx:9,20,24` (`Card`, `CardTitle`) — hardcoded `bg-[#161814]`/`border-[#262824]`/`text-[#ECEDEF]`, mesmo problema de `button.tsx`. AC: sem cor hex hardcoded sem par de tema; consumidor único (`MetasPage.tsx:197`) continua correto nos dois temas.
- [x] **T021** `[EXECUTADO 2026-08-26 — opção (a) usada: inline text-[#17708A] dark:text-[#1E88A8], sem criar token novo]` Nas variantes `outline` e `soft` de `button.tsx`, substituir `text-[#1E88A8]` (e o `border-[#1E88A8]` da `outline`) por algo que resolva para `#17708A` no claro e `#1E88A8` no escuro. **Confirmado que NÃO existe hoje nenhum token pronto para isso**: `--color-brand`/`--color-brand-hover` e `--color-admin-petrol`/`--color-admin-petrol-hover` (`index.css:123-124,161-162`) são valores fixos únicos (não redefinidos em `.dark`), não um par claro/escuro — usá-los como se fossem theme-aware seria um erro. Duas opções: (a) escrever inline `text-[#17708A] dark:text-[#1E88A8]` (padrão já usado no resto do app); (b) criar um token novo de verdade, `--text-brand`/`--color-text-brand`, definido em `:root` (`#17708A`) e `.dark` (`#1E88A8`) — mais alinhado com o objetivo do T020 de usar tokens em vez de hardcode, e **reutilizável pelo T030** (KF-002) em vez de cada arquivo repetir o hex na mão. Recomendado: opção (b). AC: contraste ≥ 4.5:1 no claro, mantém a identidade petróleo no escuro; se optar por (b), documentar o novo token em `index.css` e considerar usá-lo em T030 também (não obrigatório, mas consistente).
- [x] **T024** `[EXECUTADO 2026-08-26 — achado extra: Card usa classe bg-surface literal, não bg-[...], então também precisou de :not(.ady-card) na cláusula "main .bg-surface" separada (index.css:359), não só na cláusula de bg-[...]. Confirmado no CSS compilado: 7 ocorrências de .ady-btn/.ady-card nos 3 pares de seletores esperados]` Adicionar marker classes `.ady-btn` (em `button.tsx`, e reutilizada por **qualquer outro componente que precise escapar da mesma regra** — ver T015, `GoogleLoginButton.tsx`) e `.ady-card` (em `card.tsx`) ao `className` base dos componentes (classe simples, não-Tailwind, **NÃO usar prefixo `!` do Tailwind em nenhuma classe de cor da base** — testado e confirmado que isso quebra a sobrescrita via `className` que `Subscription.tsx:126` já usa hoje, porque `twMerge` não resolve conflito entre classe `!important` e classe normal). `.ady-btn` é uma convenção genérica de "este botão gerencia seu próprio tema, pular o bloco genérico" — não uma marca exclusiva de `button.tsx`. Estender os seletores existentes em `index.css`, sem remover nada:
  - `index.css:352` — `html:not(.dark) main [class*="bg-["]:not(.gradient-teal):not(.gradient-spark):not([class*="bg-primary-foreground"])` → adicionar `:not(.ady-btn):not(.ady-card)`.
  - `index.css:375-386` — `html:not(.dark) main button:not([type="submit"]):not(.bg-brand):not(.gradient-spark):not(.ady-calendar-shell button)` (regra base e a de `:hover`) → adicionar `:not(.ady-btn)`. Precedente direto: essa mesma técnica já protege os botões de `CalendarView.tsx` via `.ady-calendar-shell button` — usar a convenção existente, não inventar uma nova.
  AC — **corrigido na 5ª verificação (2026-08-26) para descrever o estado ATUAL real, não a generalização anterior** (`<main>` só existe via `AppLayout.tsx`/`ConectarMetaPage.tsx`/`AdminShell.tsx`; os outros 7 consumidores de `Button` só têm ancestral `.flex`, não `main` — ver `spec.md` para o cálculo completo por variante):
  (a) `variant="spark"`/`variant="destructive"` em qualquer um dos 9 consumidores: hoje o fundo (laranja/vermelho) já aparece correto, mas o texto vem forçado cinza (`#475569`) pela regra `.flex button:not(...)`; depois do T024, o texto próprio da variante (branco) deve aparecer, sem depender de estar ou não dentro de `<main>`;
  (b) `variant="outline"` em qualquer consumidor: hoje borda e texto vêm forçados para cinza neutro; depois do T024, a borda/texto petróleo (`#17708A` no claro, ver T021) devem aparecer;
  (c) `variant="default"` em `ConectarMetaPage.tsx` (tem `<main>` próprio): hoje o fundo `bg-[#1A1B17]` É forçado para branco por `main [class*="bg-["]`; depois do T024, deve manter o fundo escuro pretendido pela variante (ou o token que T020 definir para ela);
  (d) `Subscription.tsx:126` — `<Button variant="primary" className="bg-[#1E88A8] hover:bg-[#1E88A8]/80 text-white">` continua renderizando exatamente essas cores customizadas (não a cor default da variante) — **teste de regressão obrigatório, é o caso real que a verificação encontrou como risco**;
  (e) `MetasPage.tsx:197` (tem `<main>` via `AppLayout`) — `<Card className="border border-white/10 bg-admin-surface ...">` continua respeitando a customização.
- [ ] **T022 (parcial)** `[VERIFICADO DE VERDADE com navegador real via CDP em 2026-08-26 — ver KF-015]` Confirmados limpos nos dois temas: `Dashboard`, `MetasPage`, `Subscription`, `Plans` (2 bugs achados e corrigidos), `CalendarView`. Faltam os outros 20 arquivos (a maioria no módulo Estúdio/campaign-wizard, território do KF-007). `[Risco: baixo — lista corrigida de 9 para 25 arquivos após execução real do T005, 2026-08-26]` Verificar visualmente os **25 arquivos consumidores** nos dois temas, cada variante usada, **dentro e fora de `<main>`**:
  - Diretos (9): `CampaignWizard.tsx`, `InstagramPostsTab.tsx`, `Step5Review.tsx`, `ConectarMetaPage.tsx`, `Subscription.tsx`, `CalendarView.tsx`, `Plans.tsx`, `MetasPage.tsx` (inclui `Card`), `PublicoContent.tsx`.
  - Via barrel `@/components` (14): `FuryRuleDialog.tsx`, `MinhasRegras.tsx`, `ComponentsDemo.tsx`, `BrandKitPage.tsx`, `FuryConfig.tsx`, `estudio/components/{AdaptiveQuestions,CreativeFieldsForm,CreativeResult,CreativeWizard,LayoutPicker,LayoutSuggestion}.tsx`, `estudio/{CreativeStudio,GeradorCopy,GeradorImagem}.tsx`.
  - Via import relativo (2): `components/budget/{BudgetModeToggle,BudgetSuggestionCard}.tsx`.

  Nota: 16 destes 25 estão no módulo Estúdio/território do KF-007 (rebrand nunca feito) — a correção de `button.tsx` vai deixá-los tematicamente corretos, mas eles continuam com laranja Fury em outros elementos (fora do escopo desta spec, ver KF-007). Não confundir "botão ficou certo" com "tela ficou rebrandeada" nesses 16 arquivos.

  AC: cada um dos 25 arquivos marcado ✅/❌ nesta task; qualquer ❌ vira task nova antes de fechar a fase.
- [ ] **T023** `[Risco: baixo]` Comparar visualmente antes/depois de T020-T024 nas telas de T022 — confirmar que nenhum botão/card dependia acidentalmente do bloco genérico para uma aparência que a correção não reproduz (ex. algum caso onde o branco forçado era, por coincidência, o resultado desejado). AC: nenhuma diferença inesperada de cor encontrada, ou diferença documentada e aceita conscientemente.

### ✅ Checklist de saída — Fase 3

- [ ] `button.tsx` e `card.tsx` sem cor hardcoded sem par de tema, usando tokens semânticos.
- [ ] Variantes `outline`/`soft` com contraste AA no claro.
- [ ] Exceção aditiva no bloco CSS genérico (T024) implementada e confirmada por especificidade, não só visualmente.
- [ ] Todos os 25 arquivos consumidores verificados nos dois temas, dentro e fora de `<main>`.
- [ ] Nenhuma dependência oculta do bloco CSS genérico mascarando um bug em `button.tsx`/`card.tsx`.

---

## Fase 4 — KF-002: contraste `#1E88A8`-como-texto (risco baixo, troca de valor isolada por arquivo — lista corrigida em 2026-08-26)

**Atenção**: a lista abaixo já exclui os 8 falsos positivos encontrados na verificação profunda (textos dentro de cards sempre-escuros, onde `#1E88A8` está correto). Não usar a tabela original de `spec.md` antes da revisão — usar só a tabela "REVISADA" atual.

- [x] **T030** `[EXECUTADO 2026-08-26 — inclui Dashboard.tsx:632 (achado da Fase 1)]` Nos 8 arquivos abaixo (exclui `AssinaturaVencida.tsx`, tratado em T012, e exclui `button.tsx`, tratado na Fase 3), trocar `text-[#1E88A8]` por `text-[#17708A]` **somente** nas linhas confirmadas como violação real: `ConfiguracoesTabsNav.tsx:31,32`; `ForgotPasswordPage.tsx:199`; `ResetPasswordPage.tsx:138,290,299`; `RegisterFormPage.tsx:186,350`; `LoginPage.tsx:144,217`; `RegisterPage.tsx:136`; `Dashboard.tsx:163,189,211,632` (**632 é novo, achado na execução real do T002** — `<Link to="/integracoes">`, efeito visual imediato, sem mascaramento); `Metas.tsx:79,172,194,244,277,316`. **NÃO tocar** em `RoadmapPage.tsx`, `Plans.tsx`, `Subscription.tsx`, nem em `Dashboard.tsx:89,304,482,493,579` — são cards sempre-escuros onde `#1E88A8` está correto.

  **Classificação por efeito visual real (achado da 3ª verificação, 2026-08-26) — não confundir "trocado no código" com "mudou na tela"**:
  - **Efeito visual imediato** (nada mascara): todos os `<Link>`/`<a>` (`ForgotPasswordPage:199`, `ResetPasswordPage:138,299`, `RegisterFormPage:350`, `LoginPage:144,217`, `RegisterPage:136`, `Dashboard:189,632`); `ConfiguracoesTabsNav.tsx:31,32` (já usa prefixo `!` do Tailwind — **preservar o `!` na troca**: `!text-[#1E88A8]` → `!text-[#17708A]`, não virar `text-[#17708A]` sem `!`).
  - **Sem efeito visual hoje** (mascarado pelo bloco genérico — `span`/`button` sem exclusão, cor real já forçada para `#475569` cinza): `ResetPasswordPage.tsx:290`, `RegisterFormPage.tsx:186` (botões "Reenviar código", `type="button"` dentro de ancestral `.flex`); `Metas.tsx:79,172,194,244,277,316` (todos `<span>`/`<button>` sem exclusão); `Dashboard.tsx:163` (div `.icon-meta-circle`, texto não é mascarado mas o FUNDO `bg-[#1E88A8]/10` desse mesmo elemento pode estar sendo forçado a branco pelo bloco — achado à parte, fora do escopo desta task, ver nota em `plan.md`). A troca de valor nesses casos é código-correto mas **não muda nada na tela** até o bloco genérico ser tratado — não é regressão nem falha da task, é esperado.
  - **KF-006 (decisão do usuário pendente)**: as 4 ocorrências condicionais de `Metas.tsx` (79,244,277,316) mascaradas fazem o app perder o sinal visual de "bom"/"ruim"/"destaque" em modo claro HOJE — ver T034 abaixo, condicional à decisão do usuário.

  AC: `grep -rn 'text-\[#1E88A8\]' apps/web/src` retorna só usos em ícone/borda/fundo, rotas sempre-escuras (`/admin/*`), e os cards sempre-escuros explicitamente excluídos acima.
- [x] **T030b** `[EXECUTADO 2026-08-26]` Na mesma passada de T012 (Fase 2, `AssinaturaVencida.tsx`), incluir a troca de `AssinaturaVencida.tsx:67` (`hover:text-[#1E88A8]` → `hover:text-[#17708A]`) — não fazer como edição separada, para não conflitar com a retematização completa do arquivo já planejada ali.
- [ ] **T031** `[Risco: baixo]` Verificar visualmente **apenas os itens classificados como "efeito visual imediato" em T030** — os mascarados não vão mudar de aparência, não marcar como ❌ por isso. Em `LoginPage.tsx:144`, `RegisterFormPage.tsx:350` (`<Link>`, tem efeito real) — já existe `hover:text-[#17708A]` em elementos próximos; decidir conscientemente se a base virar `#17708A` também (perdendo a transição de hover) vale o ganho de AA. AC: cada arquivo da lista "efeito visual imediato" marcado ✅/❌; qualquer ❌ ou decisão de design vira nota explícita nesta task.
- [x] **T034** `[SKIPPED — decisão explícita do usuário, 2026-08-26]` Correção do bug funcional de KF-006 (sinalização de status quebrada em `Metas.tsx`) **adiada conscientemente**, não esquecida. O usuário optou por só documentar (T030 segue trocando o valor por higiene de código, mas o sinal visual de "bom/ruim"/"destaque" continua ausente em modo claro até uma spec futura tratar o bloco genérico ou aplicar esta mesma técnica). Candidato a entrar na spec 013 junto com a remoção do bloco `html:not(.dark)`.
- [ ] **T032** `[Risco: nenhum]` Rodar accesslint (ou inspeção manual de contraste) nos elementos alterados de `Dashboard.tsx` e `Metas.tsx` especificamente — são as duas telas de maior tráfego afetadas por esta fase. AC: contraste ≥ AA confirmado nesses dois arquivos antes de considerar a fase concluída.
- [ ] **T033** `[Risco: nenhum, opcional]` Itens de baixa prioridade encontrados na verificação, incluir só se sobrar tempo: `Dashboard.tsx:89` (código morto, `STATUS_CONFIG.text` nunca consumido — trocar é inofensivo mas não corrige nada visível) e `components/ProgressGoal.tsx:39-40` (componente sem nenhum consumidor — corrigir só é útil se o componente for revivido). AC: se feito, documentar; se pulado, documentar que foi pulado conscientemente (não esquecido).

### ✅ Checklist de saída — Fase 4

- [ ] 22 ocorrências corrigidas (21 na Fase 4 + 1 em `AssinaturaVencida.tsx` via T012).
- [ ] Confirmado que os 8 falsos positivos (cards sempre-escuros) **não** foram tocados.
- [ ] Nenhuma regressão visual de hierarquia/contraste nos 8 arquivos.
- [ ] Estados condicionais de `Metas.tsx` testados nos dois valores, não só o default.
- [ ] `Dashboard.tsx` e `Metas.tsx` (maior tráfego) verificados com prioridade via accesslint.

---

## Fase 5 — Remoção de código morto (risco baixo, mas destrutivo — checar antes)

- [x] **T040** `[EXECUTADO 2026-08-26, com OK explícito do usuário]` Após revalidar T003 (grep zerado no momento da remoção, não só na data da investigação), deletar `apps/web/src/components/AuthLayout.tsx` e remover sua exportação de `apps/web/src/components/index.ts`. AC: `grep -r AuthLayout apps/web/src` retorna zero após a remoção; `tsc -b` continua passando (nenhum import quebrado).

### ✅ Checklist de saída — Fase 5

- [ ] `AuthLayout.tsx` removido.
- [ ] Build não quebrou por import órfão.

---

## Fase 6 — Verificação de contraste objetiva (risco zero, ferramenta)

- [x] **T050 (parcial — rotas públicas)** `[EXECUTADO 2026-08-26]` Rodado de verdade contra o dev server local (`npm run dev`, dois problemas de ambiente pré-existentes resolvidos no caminho: `swagger-ui-express` faltando no `apps/api`, e `packages/db` com build desatualizado sem `workflowJobs`). Resultado nas 7 rotas alcançáveis sem login: `/login`, `/cadastro`, `/cadastro/formulario`, `/forgot-password`, `/reset-password`, `/onboarding/meta-authorize`, `/assinatura-vencida` — **zero violações AA**. Rota 404 (`/rota-inexistente`) **encontrou 2 violações reais, novas, fora do KF-002 original**: botão "Voltar ao início" (`bg-accent text-white`, 3.63:1) e rodapé "ady" (`text-accent/60`, 2.66:1) — nenhuma tinha sido catalogada porque não é sobre claro/escuro, é sobre o laranja da marca não ter contraste suficiente nesses dois usos específicos. **Corrigido**: botão trocado para a classe `gradient-spark` já existente (usa o tom mais escuro `#B55F02` como âncora); rodapé trocado para `text-[#B55F02] dark:text-accent`. Reauditado — zero violações. **Ainda faltam**: rotas que exigem login (`Dashboard`, `Metas`, e os 25 consumidores de `Button`) e estados de `:hover` (o accesslint audita o DOM atual, não simula hover) — exigem navegação manual ou uma sessão autenticada. Rodar `mcp__plugin_accesslint_accesslint__audit_live` (ou a skill `accesslint:accessibility-scan`) nas rotas: `/login`, `/cadastro/formulario`, `/forgot-password`, `/reset-password`, `/onboarding/meta-authorize` (estado de erro e estado de loading), `/assinatura-vencida`, uma rota inexistente para acionar `NotFoundPage`, e **as rotas dos 25 arquivos consumidores de `Button.tsx`** (Fase 3, corrigido de 9) e dos 9 arquivos de KF-002 (Fase 4, número corrigido — era citado como 12 antes da revisão) que ainda não estiverem cobertos pela lista original. Rodar em modo claro e modo escuro. AC: nenhuma violação de contraste AA (WCAG SC 1.4.3/1.4.11) reportada nos elementos alterados nas Fases 2-4; violações pré-existentes fora do escopo desta spec são anotadas separadamente, não bloqueiam esta fase.

### ✅ Checklist de saída — Fase 6

- [ ] Todas as rotas em escopo (originais + Button.tsx + KF-002) auditadas via accesslint, nos dois temas.
- [ ] Zero violação AA nos elementos alterados por esta spec.

---

## Fase 7 — Verificação manual multi-tela (risco zero, checklist)

- [ ] **T060** `[Risco: nenhum]` Para cada uma das telas abaixo, alternar entre tema claro e escuro (toggle do usuário) e confirmar visualmente que não há regressão introduzida pelas Fases 2-5: `/login`, `/cadastro`, `/cadastro/formulario`, `/forgot-password`, `/reset-password`, `/onboarding/meta-authorize` (sucesso e erro), `/assinatura-vencida`, rota 404, e as telas dos 25 arquivos de `Button.tsx` (Fase 3, corrigido de 9). AC: cada tela marcada com ✅/❌ nesta lista; qualquer ❌ vira uma task nova antes de considerar a fase concluída.
- [ ] **T061** `[Risco: nenhum]` Verificar, como amostra de controle (não exaustivo), que 2-3 telas que usam os mesmos seletores tocados na Fase 2 mas não fazem parte desta spec (`Dashboard.tsx`, `Metas.tsx` — usam `.flex button`/`span`/`p` genéricos — **não `main button`, confirmado na 5ª verificação que `<main>` não existe no `AuthenticatedShell`**) **não mudaram de aparência de forma não intencional** (a mudança intencional de KF-002/Fase 4 nessas duas telas é esperada — confirmar que só o que a Fase 4 mudou, mudou). AC: nenhuma diferença visual não explicada por esta spec.

### ✅ Checklist de saída — Fase 7

- [ ] Todas as telas em escopo aprovadas visualmente nos dois temas.
- [ ] Telas de controle sem regressão não intencional.

---

## Fase 8 — Fechamento e registro dos achados fora de escopo

- [x] **T070** `[EXECUTADO 2026-08-26]` `npm run build` (`tsc -b && vite build`) rodado de verdade — limpo, zero erro. CSS final ficou ~5KB menor após a remoção de código morto (Tailwind parou de gerar classes que só existiam em `AuthLayout.tsx`/`FuryLiveFeed.tsx`).
- [ ] **T071** `[Risco: nenhum]` Registrar formalmente, como candidato real a `specs/013-...` (**frente 1: remoção do bloco CSS** — ver nota no topo deste arquivo), o que **continua** fora do escopo: remoção real do bloco `html:not(.dark)` (Abordagem A do `plan.md`, ainda não segura). AC: achado citado em local rastreável.
- [ ] **T072** `[Risco: nenhum]` Registrar, como pendência de **contraste AA remanescente** (frente 3, NÃO é a mesma spec 013 da remoção do bloco CSS): **KF-004** (`disabled:bg-gray-50` hardcoded em `input.tsx`/`select.tsx`, campo desabilitado fica cinza claro no escuro), `dropdown-menu.tsx`/`form.tsx` (theme-unaware mas código morto), **KF-006** (sinalização de status quebrada em `Metas.tsx`, adiada por decisão do usuário — T034), **KF-009** (~44 ocorrências extras de `text-brand`/`text-admin-petrol` como texto, achado da 8ª verificação, não classificadas). AC: os 4 achados citados em local rastreável, não perdidos; registrado que são a MESMA família de problema do KF-002 desta spec, mas propositalmente fora da execução por volume.
- [ ] **T073** `[Risco: nenhum, achado da 6ª verificação, 2026-08-26]` Registrar **KF-007** como candidato a uma spec própria de **conclusão de rebrand** (frente 2, NÃO é a spec 013 nem a pendência de contraste acima — é troca de cor de marca, sem nenhuma relação com CSS/tema): laranja Fury original nunca rebrandeado, mais de 90 ocorrências em ~25 arquivos, concentradas em `pages/estudio/*` e `components/campaign-wizard/*`, mais o design system compartilhado `input.tsx`/`select.tsx`/`dialog.tsx`. Maior em volume que esta spec inteira. Ver `spec.md` para a lista completa. AC: achado registrado em local rastreável, não perdido; usuário ciente do tamanho antes de decidir quando tratar.
- [x] **T074** `[EXECUTADO 2026-08-26, com OK explícito do usuário — hooks/useFuryLiveFeed.ts também removido junto, órfão sem outro consumidor]` `apps/web/src/components/FuryLiveFeed.tsx` — confirmado código morto (mesmo padrão de `AuthLayout.tsx`, zero imports ativos). Pode ser removido na mesma leva da T040 por conveniência, revalidando o grep antes. AC: se removido, `grep -r FuryLiveFeed apps/web/src` retorna zero e `tsc -b` continua passando; se não removido nesta fase, documentado como pendência.

### ✅ Checklist de saída — Fase 8

- [x] Build limpo.
- [x] Achados remanescentes (remoção do bloco CSS, outros componentes theme-unaware) registrados, não perdidos.

---

## Fase 9 — Execução real pós-handoff (continuação da sessão, 2026-08-26) — achados e correções fora do plano original

Esta fase não estava no plano original (`plan.md`) — é o registro do que foi encontrado e corrigido durante a execução real das fases anteriores, incluindo dois bugs reportados ao vivo pelo usuário depois que ele testou o resultado no navegador. Ver `spec.md` KF-016 a KF-021 para o diagnóstico completo de cada um.

- [x] **T080** `[EXECUTADO 2026-08-26]` Corrigido KF-016: regras genéricas de fundo (`main [class*="bg-["]`/`bg-zinc`/`bg-slate`/`bg-neutral`/`bg-stone`/`bg-black`/`.bg-surface`) e de tipografia (`h1`-`h4`, `p`/`span`/`label`/`.text-muted-foreground`) em `index.css` passaram de `:not(.ady-decor)` para `:not(.ady-decor):not(.ady-decor *)` (ou só `:not(.ady-decor *)` nas de tipografia) — cobre descendentes, não só o elemento com a classe. Sem isso, texto dentro de um card `ady-decor` continuava sendo repintado pela regra genérica. AC: confirmado via `accesslint` que os cards do Painel pararam de ter texto quase invisível.
- [x] **T081** `[EXECUTADO 2026-08-26]` Corrigido KF-017: regra duplicada `html:not(.dark) h1/h2/h3` (`index.css`, resquício do fix de KF-012) recebeu `:not(.gradient-teal *)` — faltava essa exclusão que a regra "irmã" original já tinha. Sem isso, o `<h1>` do banner do Planejador IA (`IdleStatus.tsx:36`, "Seu mês de conteúdo, planejado em um clique.") ficava quase preto sobre fundo teal escuro. **Achado ao vivo pelo usuário** ("no planejador de ia o titulo do card que está escuro tem que ficar branco"). AC: confirmado via screenshot real nos dois temas + accesslint.
- [x] **T082** `[EXECUTADO 2026-08-26 — diagnóstico fornecido pelo usuário, verificado no código antes de aplicar]` Corrigido KF-018: regra `main {background-color:#EBEEF0!important}` (sem exceção) passou a `main:not(.ady-decor)`; `<main>` de `ConectarMetaPage.tsx` marcado com `ady-decor` (não define fundo próprio — fica transparente de propósito para deixar o gradiente decorativo atrás aparecer). Resolve o corte/faixa visível no gradiente de fundo no modo claro, bug de stacking context (`z-index:auto` do gradiente vs. `z-10` explícito de `<header>`/`<main>`) que tinha sobrevivido a várias rodadas anteriores de ajuste (errado) de opacidade/posição do gradiente. AC: confirmado via screenshot real (CDP) nos dois temas, gradiente contínuo sem corte.
- [x] **T083** `[EXECUTADO 2026-08-26 — decisão do usuário: "tematizar de vez" em vez de manter o stopgap `ady-decor` ou reverter]` Retematização completa dos 6 blocos que usam a constante `SURFACE` em `Dashboard.tsx` (Métricas, Engajamento no Instagram, Desempenho da semana, Campanhas ativas + 2 outros) — substituídos os pares de cor hardcoded (`text-[#ECEDEF]`, `text-[#9A9D96]`, `text-[#8A8D86]`, `bg-[#1F211D]`, `border-[#262824]`) por pares explícitos `slate-*`/`dark:#hex`, replicando o padrão que `HeroStrip` já usava. Skeleton de carregamento (`animate-pulse bg-[#1F211D]`) também recebeu par claro (`bg-gray-200` — não `bg-slate-200`, que colidiria com a regra genérica de `main [class*="bg-slate"]`). Gráfico Recharts (`WeeklyChart`) migrado de cores JS hardcoded (`C.muted`, `C.cardAlt`, etc.) para `var(--color-text-tertiary)`/`var(--color-surface-secondary)`/`var(--color-border-light)` — CSS custom properties funcionam em atributos de apresentação SVG em navegadores modernos. AC: `accesslint` zero violações nos dois temas em `/dashboard` (confirmado após 3 rodadas de correção, ver T084).
- [x] **T084** `[EXECUTADO 2026-08-26]` Durante a verificação de T083, `accesslint` encontrou um bug real adicional: o estado vazio do gráfico (`isEmpty`, sem dados reais) renderiza eixos com rótulos "dia 1"–"dia 7" cobertos por uma camada semi-transparente (`bg-white/70`, a mensagem "Aguardando dados...") — a composição visual lava o texto do eixo para ~1.17:1 de contraste no modo claro (decorativo, sem informação real já que a mensagem por cima é o conteúdo relevante). Corrigido com `aria-hidden` no wrapper do gráfico quando `isEmpty`, em vez de forçar mais contraste num texto que não carrega informação nesse estado. AC: `accesslint` zero violações.
- [x] **T085** `[EXECUTADO 2026-08-26, pedido explícito do usuário]` Consolidada a constante `SURFACE` (`Dashboard.tsx`) para ser autocontida — o par `border-slate-200 bg-white dark:border-[#262824] dark:bg-[#161814]` (antes repetido em cada um dos 6 usos) passou a fazer parte da própria constante. Qualquer card novo que usar `${SURFACE}` como base nasce correto nos dois temas sem repetir nada. AC: `accesslint` continua zero violações após a consolidação; confirmado visualmente que nada mudou de aparência.
- [x] **T086** `[EXECUTADO 2026-08-26]` Corrigido KF-019 (Faísca `#CF6F03` com texto branco reprova AA, 3.54:1): `variant="spark"` de `button.tsx` e `--gradient-spark` (`index.css`, ambos os blocos de tema) escurecidos para `#B55F02`/`#9A4F02` (branco sobre eles mede 4.56:1/6.01:1). Sobrescrita indevida (`text-slate-900` no claro) removida do botão "Novo Post/Campanha" em `CalendarView.tsx` — **achado ao vivo pelo usuário** ("na tela de calendario o botão de criar também tem que ter o texto em branco"). Mesma cor também corrigida em dois badges de `RoadmapPage.tsx` (`ring`/badge "Nós estamos aqui"), fora do escopo de tema mas mesmo bug de contraste.
- [x] **T087** `[EXECUTADO 2026-08-26]` Corrigido KF-020 (padrão sistêmico `bg-brand`/`bg-[#1E88A8]` sólido + texto branco em repouso, 4.08:1): trocado para `#17708A` (5.64:1) em `Configuracoes.tsx`, `GoogleIntegrationCard.tsx` (×3), `IntegracoesContent.tsx` (×4), `Plans.tsx` (×3), `PeriodSelector.tsx`, e na variante `primary` de `button.tsx` — que por sua vez corrige de uma vez todos os botões de submit das telas de autenticação (`LoginPage`, `RegisterPage`, `RegisterFormPage`, `ForgotPasswordPage`, `ResetPasswordPage`, `ResetPasswordSuccessPage`) e `AssinaturaVencida.tsx`, onde só o estado de repouso estava errado (hover já usava `#17708A` corretamente). Hover reajustado para `#145E74` nesses casos para manter feedback visual.
- [x] **T088** `[EXECUTADO 2026-08-26 — prioridade citada no KF-009 original]` `Sidebar.tsx:86,92` — item ativo da navegação trocado de `text-brand` (fixo, `#1E88A8`, 3.44-3.83:1 conforme fundo) para par `text-[#17708A] dark:text-[#2A9BC0]` (`#2A9BC0`: novo tom usado especificamente para petróleo-sobre-fundo-quase-preto, ver justificativa no próprio commit). `SidebarUserCard.tsx` (avatar circular) recebeu o mesmo tratamento. Botão de recolher barra lateral (só ícone, sem texto) ganhou `aria-label` — bug de acessibilidade encontrado no caminho, não relacionado a cor. `IntegracoesContent.tsx`: `<select>` de conta de anúncios ativa não tinha rótulo associado ao campo — ligado via `id`/`htmlFor`. Resto das ~44 ocorrências de KF-009 **continua fora do escopo** (não expandido).
- [x] **T089** `[INVESTIGADO 2026-08-26, fora do escopo por decisão do usuário]` KF-021: `/admin/login` não fica realmente em modo escuro forçado (race entre `ForceDarkMode.tsx` e `ThemeProvider.tsx` na montagem). Usuário confirmou que a tela é sempre-escura por design, sem necessidade de modo claro — nenhuma correção de código feita, só o diagnóstico ficou registrado em `spec.md`.
- [x] **T090** `[EXECUTADO 2026-08-26]` Verificação real via `accesslint` (não leitura de código) nos dois temas, em rotas autenticadas e não-autenticadas tocadas nesta fase: `/dashboard`, `/dashboard/metas`, `/configuracoes`, `/configuracoes/integracoes`, `/planos`, `/login`, `/cadastro`, `/cadastro/formulario`, `/forgot-password`, `/reset-password`, `/planejador`, `/calendario`. Todas com zero violações sérias/críticas ao final da fase. Lição operacional registrada: o `accesslint` reaproveita abas existentes que combinam com a URL — depois de uma correção, sempre fechar a aba (ou garantir que é uma aba nova) antes de reauditar, senão o resultado pode vir de uma renderização anterior.

### ✅ Checklist de saída — Fase 9

- [x] KF-016 a KF-020 corrigidos e verificados via `accesslint` real (não só leitura de código).
- [x] KF-021 investigado e documentado, deliberadamente fora do escopo por decisão do usuário.
- [x] Os dois bugs reportados ao vivo pelo usuário (Planejador IA, Calendário) corrigidos na causa raiz, não só no sintoma.
- [x] `SURFACE` (Dashboard.tsx) consolidada — cards novos herdam tema correto sem esforço extra.
- [x] `tsc --noEmit` limpo depois de cada rodada de edição.

---

## ✅ Checklist geral (master — todas as fases)

- [x] Fase 1 — Auditoria prévia concluída (incluindo T005, lista de consumidores de `Button`/variantes).
- [x] Fase 2 — 5 correções pontuais concluídas (T010-T014), incluindo T030b (`AssinaturaVencida.tsx:67`) e T015 (`GoogleLoginButton.tsx`, achado da 4ª verificação — depende de T024 ter rodado antes).
- [x] Fase 3 — `Button.tsx` **e** `Card.tsx` theme-aware, exceção aditiva no CSS (T024, marker class `.ady-btn`/`.ady-card`, SEM `!important` nas classes de cor) implementada, teste de regressão de `Subscription.tsx:126`/`MetasPage.tsx:197` (customização via `className` continua funcionando) passou, e os **25 arquivos consumidores** verificados (via `accesslint` real, não só leitura — Fase 9 fechou os que faltavam).
- [x] Fase 4 — KF-002 corrigido nas 22 ocorrências reais, **8 falsos positivos confirmadamente não tocados**; KF-006 (sinalização de status quebrada em `Metas.tsx`) conscientemente adiado (T034 SKIPPED por decisão do usuário) — registrado como pendência, não esquecido.
- [x] Fase 5 — `AuthLayout.tsx` e `FuryLiveFeed.tsx` removidos (T040, T074).
- [x] Fase 6 — Contraste AA confirmado via `accesslint` em todas as rotas em escopo, incluindo as autenticadas (fechado na Fase 9/T090).
- [x] Fase 7 — Verificação manual multi-tema concluída (via `accesslint` + screenshot real por CDP), sem regressão em telas de controle.
- [x] Fase 8 — Build limpo, achados remanescentes registrados (KF-004, KF-006, KF-007, KF-009 parcial).
- [x] Fase 9 (fora do plano original) — 6 achados novos (KF-016 a KF-021) durante a execução real, incluindo 2 bugs reportados ao vivo pelo usuário, todos corrigidos ou conscientemente registrados como fora de escopo.
- [x] **Nenhuma linha do bloco `index.css:322-462` foi removida** — só adições pontuais fora dele (confirmado, `git diff apps/web/src/index.css` só mostra `:not(...)` acrescentados e trocas de valor hex, nenhuma regra excluída).
- [ ] `plan.md` → "Checklist geral do plano" (seção correspondente) — **não revisado nesta rodada**, `plan.md` ainda reflete só o plano original, não a Fase 9. Ver `HANDOFF.md` para o estado real consolidado.

## Notas

- **Estado real em 2026-08-26 (fim da sessão de continuação): execução completa.** Fases 1-8 do plano original executadas (checkboxes `[x]` atualizados ao longo da sessão), mais uma Fase 9 não planejada com 6 achados novos, dois deles reportados ao vivo pelo usuário depois de testar no navegador. Ver `HANDOFF.md` para o resumo consolidado e o que continua genuinamente em aberto.
- Itens que permanecem `[ ]` de propósito (não são esquecimento): T031/T033 (baixa prioridade, documentados como não feitos), T071 (remoção do bloco CSS — decisão consciente de não fazer nesta spec), a linha de `plan.md` acima.
- A remoção/redução real do bloco `html:not(.dark)` (Abordagem A do `plan.md`) **não está nesta lista** — só se torna segura de planejar depois que uma spec futura (013) auditar e corrigir os 26 arquivos mapeados na Fase 1.
- Ordem de execução sugerida: Fase 1 → T010-T014 (Fase 2) podem rodar em paralelo com T020-T023 (Fase 3) — arquivos disjuntos — **mas T024 (Fase 3) precisa terminar antes de T015 (Fase 2)**, já que T015 depende da marker class `.ady-btn` já estar excluída em `index.css` — não são realmente independentes, apesar de estarem em fases numeradas diferentes → Fase 4 (usa a lista revalidada em T002) → Fase 5 → Fase 6 → Fase 7 → Fase 8.
